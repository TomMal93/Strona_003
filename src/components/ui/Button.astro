---
interface Props {
  variant?: "primary" | "secondary" | "accent";
  href?: string;
  type?: "button" | "submit" | "reset";
  class?: string;
}

const { variant = "primary", href, type = "button", class: className } = Astro.props;

const Tag = href ? "a" : "button";

const baseClasses =
  "inline-flex items-center justify-center rounded-(--radius-md) px-6 py-3 font-semibold transition-colors duration-200 focus-visible:outline-2 focus-visible:outline-offset-2 cursor-pointer";

const variantClasses: Record<string, string> = {
  primary:
    "bg-(--color-primary) text-(--color-text-inverse) hover:bg-(--color-primary-dark) focus-visible:outline-(--color-primary)",
  secondary:
    "bg-(--color-secondary) text-(--color-text-inverse) hover:bg-(--color-secondary-light) focus-visible:outline-(--color-secondary)",
  accent:
    "rounded-(--radius-lg) border border-white/12 px-7 py-4 text-base font-medium tracking-wide text-white/70 transition-all duration-300 hover:border-white/25 hover:text-white focus-visible:outline-(--color-accent)/50",
};

const classes = [baseClasses, variantClasses[variant], className]
  .filter(Boolean)
  .join(" ");
---

<Tag
  class={classes}
  {...href ? { href } : { type }}
>
  <slot />
</Tag>
