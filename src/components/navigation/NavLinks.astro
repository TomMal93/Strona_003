---
import NavLinkList from "./NavLinkList.astro";
import { desktopNavLinks } from "../../data/navigation";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<NavLinkList links={desktopNavLinks} listClass={`flex items-center gap-8 ${className ?? ""}`.trim()} />
