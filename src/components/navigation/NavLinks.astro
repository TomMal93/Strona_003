---
import NavLinkList from "./NavLinkList.astro";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<NavLinkList listClass={`flex items-center gap-8 ${className ?? ""}`.trim()} />
